version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    deploy:
      resources:
        limits:
          memory: 256M # Limit frontend to 256MB of memory

  backend:
    build:
      context: ./backend
    ports:
      - "9000:9000"
    environment:
      SCALA_BACKEND_CONFIG: "production" # Set your environment variables here
      JAVA_OPTS: "-Xms128M -Xmx256M -XX:+UseG1GC" 
    deploy:
      resources:
        limits:
          memory: 512M # Limit backend to 256MB of memory
    networks:
      - poker-network

networks:
  poker-network:
    driver: bridge
